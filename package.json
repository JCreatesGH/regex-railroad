{
  "name": "regex-railroad",
  "version": "0.2.0",
  "description": "Parse a regular expression and render it as a readable SVG railroad diagram.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": { "regex-railroad": "dist/cli.js" },
  "sideEffects": false,
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": { "test": "vitest run", "build": "tsc -p tsconfig.json", "prepare": "npm run build" },
  "keywords": ["regex", "railroad", "diagram", "svg", "visualization", "ast", "lookahead", "cli"],
  "author": "Joshua John <josh202@gmail.com>",
  "license": "MIT",
  "devDependencies": { "vitest": "^4.0.0", "typescript": "^5.4.0", "@types/node": "^22.19.21" }
}
